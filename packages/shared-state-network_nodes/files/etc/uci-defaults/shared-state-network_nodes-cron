#!/bin/sh

unique_append()
{
	[ -f "$2" ] && grep -qF "$1" "$2" || echo "$1" >> "$2"
}

unique_append \
	'*/5 * * * * ((sleep $(($(hexdump -n 2 -e \"%u\" /dev/urandom) % 120)); shared-state-multiwriter sync network_nodes &> /dev/null)&)'\
	/etc/crontabs/root
