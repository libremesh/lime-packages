#!/bin/sh

unique_append()
{
	[ -f "$2" ] && grep -qF "$1" "$2" || echo "$1" >> "$2"
}

unique_append \
	'*/20 * * * * ((sleep $(($(hexdump -n 2 -e \"%u\" /dev/urandom) % 600)); check-date-http &> /dev/null)&)'\
	/etc/crontabs/root
