#!/bin/sh

[ "$ACTION" = "post" ] || exit 0

hostname="$(uci get system.@system[0].hostname)"

[ -f /www/app/index.html ] &&
	sed -i "s,\(<title>\).*\(</title>\),\1${hostname}\2,g" /www/app/index.html
