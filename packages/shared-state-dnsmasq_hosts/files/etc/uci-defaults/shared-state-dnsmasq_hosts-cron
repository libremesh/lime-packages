#!/bin/sh

unique_append()
{
	[ -f "$2" ] && grep -qF "$1" "$2" || echo "$1" >> "$2"
}

unique_append \
	'*/5 * * * * ((sleep $(($RANDOM % 120)); shared-state sync dnsmasq-hosts &> /dev/null)&)'\
	/etc/crontabs/root
